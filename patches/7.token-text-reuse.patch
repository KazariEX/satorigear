diff --git a/src/emit-parser.ts b/src/emit-parser.ts
index ddedf1e..ab2d070 100644
--- a/src/emit-parser.ts
+++ b/src/emit-parser.ts
@@ -2639,7 +2639,6 @@ ${e.soa ? `  tokenize(source);
   const _toks = tokenize(source);
   const _n = _toks.length;
   if (tkCap < _n + 1) growTok(_n + 1);
-  tkText.length = 0;
   for (let _i = 0; _i < _n; _i++) {
     const _t = _toks[_i];
     tkK[_i] = _t.k; tkT[_i] = _t.t; tkOff[_i] = _t.offset; tkEnd[_i] = _t.offset + _t.text.length;
@@ -2647,4 +2646,5 @@ ${e.soa ? `  tokenize(source);
     tkDp[_i] = 0; tkPd[_i] = 0;
     tkText[_i] = _t.text;
   }
+  tkText.length = _n;
   tokN = _n;`}
@@ -2678,10 +2678,10 @@ function tokensInto(source: string, tokens: readonly ExternalToken[]) {
   negFrom = 0x7fffffff;
   const n = tokens.length;
   if (tkCap < n + 1) growTok(n + 1);
-${e.soa ? '' : '  tkText.length = 0;'}
   for (let i = 0; i < n; i++) {
     writeExternalToken(i, tokens[i]);
   }
+${e.soa ? '' : '  tkText.length = n;'}
   tokN = n;
   docEmptyPops = [];
 }
@@ -4018,7 +4018,7 @@ ${e.soa ? String.raw`  // ── M1: WINDOWED re-lex ──
   }
   tkK = altK!; tkT = altT!; tkOff = altOff!; tkEnd = altEnd!; tkFl = altFl!;
   { const _d = tkDp; tkDp = altDp!; altDp = _d; const _q = tkPd; tkPd = altPd!; altPd = _q; }
-  tkText = altText; tkText.length = 0;
+  tkText = altText;
   altK = oK; altT = oT; altOff = oOff; altEnd = oEnd; altFl = oFl;
   altText = oText;
   docLex.length = 0;   // a FULL relex re-derives all lexer diagnostics (none, for
