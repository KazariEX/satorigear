diff --git a/src/emit-parser.ts b/src/emit-parser.ts
index 20df7c3..5305316 100644
--- a/src/emit-parser.ts
+++ b/src/emit-parser.ts
@@ -1115,4 +1115,94 @@ export function emitJsLexer(grammar: CstGrammar): string | null {
   });
 }
-
+
+export function emitJsPackedLexer(grammar: CstGrammar): string {
+  grammar = withAwaitYield(grammar);
+  const a = analyze(grammar);
+  const st = a.symtab;
+  if (st.kwLitKind.size > 0 || st.puLitKind.size > 0) {
+    throw new Error('Packed lexers require a grammar without literal kinds');
+  }
+  const lexer = emitSoaLexer(grammar, {
+    typeKind: st.typeKind, kwLitKind: st.kwLitKind, puLitKind: st.puLitKind,
+    KIND_PUNCT: st.KIND_PUNCT, KIND_NAMED_FALLBACK: st.KIND_NAMED_FALLBACK,
+  });
+  if (lexer === null) {
+    throw new Error('Packed lexers require an emitted lexer');
+  }
+
+  const kindCases = [...st.typeKind]
+    .map(([name, kind]) => `    case ${J(name)}: return ${kind};`)
+    .join('\n');
+  return `// GENERATED by src/emit-parser.ts — do not edit. Specialized packed lexer for grammar ${J(grammar.name ?? '')}.
+const K_PUNCT = ${st.KIND_PUNCT};
+const K_TEMPLATE_HEAD = ${st.KIND_TEMPLATE_HEAD};
+const K_TEMPLATE_MIDDLE = ${st.KIND_TEMPLATE_HEAD + 1};
+const K_TEMPLATE_TAIL = ${st.KIND_TEMPLATE_HEAD + 2};
+const K_NAMED_MIN = ${st.KIND_NAMED_MIN};
+const K_NAMED_FALLBACK = ${st.KIND_NAMED_FALLBACK};
+const K_TPL_TOKEN = ${a.templateTokenName ? st.typeKind.get(a.templateTokenName) : -1};
+
+let tkK = new ${a.kArr}(4096);
+let tkT = new ${a.tArr}(4096);
+let tkOff = new Int32Array(4096);
+let tkEnd = new Int32Array(4096);
+let tkFl = new Uint8Array(4096);
+let tkDp = new Uint8Array(4096);
+let tkPd = new Uint16Array(4096);
+let tkCap = 4096;
+let tokN = 0;
+let docPieces: string[] | null = null;
+let docPieceOff: number[] | null = null;
+let docLen = 0;
+let docFlat: string | null = null;
+let docCur = 0;
+let srcLenP1 = 1;
+let negFrom = 0x7fffffff;
+const recovering = false;
+const docLex: { offset: number; end: number; kind: number; ch: string }[] = [];
+const docEmptyPops: number[] = [];
+let altK: ${a.kArr} | null = null;
+let altT: ${a.tArr} | null = null;
+let altOff: Int32Array | null = null;
+let altEnd: Int32Array | null = null;
+let altFl: Uint8Array | null = null;
+let altDp: Uint8Array | null = null;
+let altPd: Uint16Array | null = null;
+let altN = 0;
+
+function growTok() {
+  tkCap *= 2;
+  const k = new ${a.kArr}(tkCap); k.set(tkK); tkK = k;
+  const t = new ${a.tArr}(tkCap); t.set(tkT); tkT = t;
+  const start = new Int32Array(tkCap); start.set(tkOff); tkOff = start;
+  const end = new Int32Array(tkCap); end.set(tkEnd); tkEnd = end;
+  const flags = new Uint8Array(tkCap); flags.set(tkFl); tkFl = flags;
+  const templateDepth = new Uint8Array(tkCap); templateDepth.set(tkDp); tkDp = templateDepth;
+  const parenDepth = new Uint16Array(tkCap); parenDepth.set(tkPd); tkPd = parenDepth;
+}
+
+${lexer}
+
+export function tokenKind(type: string) {
+  switch (type) {
+${kindCases}
+    default: return K_NAMED_FALLBACK;
+  }
+}
+
+export const packedTokenStride = 4;
+
+export function tokenizePacked(source: string) {
+  lexSource(source);
+  const tokens = new Array(tokN * packedTokenStride);
+  for (let index = 0, offset = 0; index < tokN; index++, offset += packedTokenStride) {
+    tokens[offset] = tkK[index];
+    tokens[offset + 1] = tkOff[index];
+    tokens[offset + 2] = tkEnd[index];
+    tokens[offset + 3] = tkFl[index];
+  }
+  return tokens;
+}`;
+}
+
 export function emitJsParser(grammar: CstGrammar, lexSrc: string | null): string {
