diff --git a/src/emit-lexer.ts b/src/emit-lexer.ts
index c9f2921..89a5c93 100644
--- a/src/emit-lexer.ts
+++ b/src/emit-lexer.ts
@@ -9,6 +9,6 @@
 //
-// Scope: grammars WITHOUT markup / indent / newline modes (those state machines stay
-// interpreter-only for now) — emitLexer returns null for them and the caller falls back
-// to importing createLexer. The emitted token stream is REQUIRED to be identical to
-// createLexer's (same fields, same errors); test/emit-lexer-verify.ts gates it.
+// Scope: token-stream grammars and the simple newline-only mode. Markup, indentation,
+// comments and flow-suspended newlines keep using the interpreter. The emitted token
+// stream is REQUIRED to be identical to createLexer's (same fields, same errors);
+// test/emit-lexer-verify.ts gates it.
 import type { CstGrammar, TokenDecl } from './types.ts';
@@ -48,4 +48,7 @@ const nonAsciiWsConsume = (v: string, cont: boolean, indent: string): string =>
 export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | null {
-  // Out of scope: the markup / indentation / newline state machines.
-  if (grammar.markup || grammar.indent || grammar.newline) return null;
+  const newline = grammar.newline;
+  const flowOpen = newline?.flowOpen ?? [];
+  const flowClose = newline?.flowClose ?? [];
+  // Newline-only grammars need no stack when comments and flow suspension are absent.
+  if (grammar.markup || grammar.indent || newline?.comment || flowOpen.length > 0 || flowClose.length > 0) return null;
   if (grammar.tokens.some(t => tokenBlockPatternSource(t) || t.blockOnly)) return null;
@@ -83,2 +86,5 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   const kOf = (name: string) => st.typeKind.get(name) ?? st.KIND_NAMED_FALLBACK;
+  const kNewline = newline ? kOf(newline.token) : -1;
+  const hardBreak = newline?.hardBreak;
+  const kHardBreak = hardBreak ? kOf(hardBreak.token) : -1;
   const tOf = (text: string) => (isKeywordLiteral(text) ? st.kwLitKind.get(text) : st.puLitKind.get(text)) ?? 0;
@@ -104,3 +110,3 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   const matchers = grammar.tokens
-    .filter(t => t.name !== templateToken?.name)
+    .filter(t => t.name !== templateToken?.name && t.name !== newline?.token)
     .map((t, idx) => ({
@@ -109,2 +115,3 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
       re: `LXRE_${idx}_${t.name.replace(/[^A-Za-z0-9_]/g, '_')}`,
+      spanFn: `lxSpan_${idx}_${t.name.replace(/[^A-Za-z0-9_]/g, '_')}`,
       pattern: tokenPatternSource(t),
@@ -113,3 +120,8 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
       isRegex: t.flags.includes('regex'),
-      first: tokenPatternFirstCharSet(t),
+      first: t.delimitedSpan
+        ? {
+            ascii: new Set(t.delimitedSpan.markers.map(marker => marker.charCodeAt(0)).filter(code => code < 128)),
+            nonAscii: t.delimitedSpan.markers.some(marker => marker.charCodeAt(0) >= 128),
+          }
+        : tokenPatternFirstCharSet(t),
       k: kOf(t.name),
@@ -117,2 +129,3 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
       loop: tokenPatternCharLoop(t),
+      span: t.delimitedSpan,
     }));
@@ -120,3 +133,30 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   emit(`// ── Emitted lexer (emit-lexer.ts): specialized tokenize for this grammar ──`);
-  for (const m of matchers) emit(`const ${m.re} = new RegExp(${J(`(?:${m.pattern})`)}, ${J(m.flags)});`);
+  for (const m of matchers) {
+    if (m.span) {
+      const markers = m.span.markers.map(marker => `marker === ${marker.charCodeAt(0)}`).join(' || ');
+      emit(`function ${m.spanFn}(source: string, start: number) {`);
+      emit(`  const marker = source.charCodeAt(start);`);
+      emit(`  if (!(${markers}) || source.charCodeAt(start - 1) === marker) return -1;`);
+      emit(`  let openerEnd = start + 1;`);
+      emit(`  while (source.charCodeAt(openerEnd) === marker) openerEnd++;`);
+      emit(`  const openerLength = openerEnd - start;`);
+      emit(`  if (openerLength < ${m.span.minLength}) return -1;`);
+      emit(`  let pos = openerEnd;`);
+      emit(`  while (pos < source.length) {`);
+      if (!m.span.multiline) {
+        emit(`    if (source.charCodeAt(pos) === 10 || source.charCodeAt(pos) === 13) return -1;`);
+      }
+      emit(`    if (source.charCodeAt(pos) !== marker) { pos++; continue; }`);
+      emit(`    let runEnd = pos + 1;`);
+      emit(`    while (source.charCodeAt(runEnd) === marker) runEnd++;`);
+      emit(`    if (runEnd - pos === openerLength) return runEnd;`);
+      emit(`    pos = runEnd;`);
+      emit(`  }`);
+      emit(`  return -2;`);
+      emit(`}`);
+    }
+    else {
+      emit(`const ${m.re} = new RegExp(${J(`(?:${m.pattern})`)}, ${J(m.flags)});`);
+    }
+  }
   emit(`const LX_WS = /\\s+/y;`);
@@ -258,3 +298,3 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   // lexer ever sets; comment/multilineFlow stamps belong to fallback-only grammars).
-  emit(`function tokenize(source: string) {`);
+  emit(`function lexSource(source: string) {`);
   emit(`  docPieces = [source]; docPieceOff = [0]; docLen = source.length;`);
@@ -290,2 +330,6 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   emit(`  let pendingNl = false;`);
+  if (newline) {
+    emit(`  let lineStart = srcBase === 0;`);
+    emit(`  let emittedContent = pvK >= 0;`);
+  }
   emit(`  let extraFl = 0;`);
@@ -312,2 +356,5 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   emit(`    pendingNl = false;`);
+  if (newline) {
+    emit(`    if (k !== ${kNewline}) emittedContent = true;`);
+  }
   emit(`    pvK = k; pvT = t;`);
@@ -387,15 +434,67 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   emit(resyncRetractLine('    '));
+  if (newline) {
+    emit(`    if (lineStart) {`);
+    emit(`      let p = pos;`);
+    emit(`      while (p < n && source.charCodeAt(p) === 32) p++;`);
+    emit(`      if (p >= n) { pos = p; lineStart = false; continue; }`);
+    emit(`      const ch = source.charCodeAt(p);`);
+    emit(`      if (ch === 10 || ch === 13) {`);
+    emit(`        pos = p + 1;`);
+    emit(`        if (ch === 13 && pos < n && source.charCodeAt(pos) === 10) pos++;`);
+    emit(`        continue;`);
+    emit(`      }`);
+    emit(`      if (ch === 9) {`);
+    emit(`        let b = p;`);
+    emit(`        while (b < n && (source.charCodeAt(b) === 32 || source.charCodeAt(b) === 9)) b++;`);
+    emit(`        if (b >= n) { pos = b; continue; }`);
+    emit(`        const bc = source.charCodeAt(b);`);
+    emit(`        if (bc === 10 || bc === 13) {`);
+    emit(`          pos = b + 1;`);
+    emit(`          if (bc === 13 && pos < n && source.charCodeAt(pos) === 10) pos++;`);
+    emit(`          continue;`);
+    emit(`        }`);
+    emit(`      }`);
+    emit(`      pos = p;`);
+    emit(`      if (emittedContent) tkPush(${kNewline}, 0, pos, pos);`);
+    emit(`      lineStart = false;`);
+    emit(`      continue;`);
+    emit(`    }`);
+  }
   emit(`    const cc = source.charCodeAt(pos);`);
-  emit(`    // whitespace: ASCII \\s run by char loop; a non-ASCII candidate falls back to the regex`);
-  emit(`    if (cc === 32 || (cc >= 9 && cc <= 13)) {`);
-  emit(`      let wc = cc;`);
-  emit(`      do {`);
-  emit(`        if (wc === 10 || wc === 13) pendingNl = true;`);   // JS line terminators LF/CR (LS/PS via the \\s regex below)
-  emit(`        pos++;`);
-  emit(`        wc = source.charCodeAt(pos);`);
-  emit(`      } while (wc === 32 || (wc >= 9 && wc <= 13));`);
-  emit(`${nonAsciiWsConsume('wc', false, '      ')}`);
-  emit(`      continue;`);
-  emit(`    }`);
-  emit(`${nonAsciiWsConsume('cc', true, '    ')}`);
+  if (newline) {
+    emit(`    if (cc === 32 || cc === 9) {`);
+    if (hardBreak) {
+      emit(`      if (cc === 32) {`);
+      emit(`        let end = pos + 1;`);
+      emit(`        while (source.charCodeAt(end) === 32) end++;`);
+      emit(`        if (end - pos >= ${hardBreak.minSpaces} && (source.charCodeAt(end) === 10 || source.charCodeAt(end) === 13)) {`);
+      emit(`          tkPush(${kHardBreak}, 0, pos, end);`);
+      emit(`          pos = end;`);
+      emit(`          continue;`);
+      emit(`        }`);
+      emit(`      }`);
+    }
+    emit(`      pos++;`);
+    emit(`      continue;`);
+    emit(`    }`);
+    emit(`    if (cc === 10 || cc === 13) {`);
+    emit(`      pos++;`);
+    emit(`      if (cc === 13 && pos < n && source.charCodeAt(pos) === 10) pos++;`);
+    emit(`      lineStart = true;`);
+    emit(`      continue;`);
+    emit(`    }`);
+  } else {
+    emit(`    // whitespace: ASCII \\s run by char loop; a non-ASCII candidate falls back to the regex`);
+    emit(`    if (cc === 32 || (cc >= 9 && cc <= 13)) {`);
+    emit(`      let wc = cc;`);
+    emit(`      do {`);
+    emit(`        if (wc === 10 || wc === 13) pendingNl = true;`);   // JS line terminators LF/CR (LS/PS via the \\s regex below)
+    emit(`        pos++;`);
+    emit(`        wc = source.charCodeAt(pos);`);
+    emit(`      } while (wc === 32 || (wc >= 9 && wc <= 13));`);
+    emit(`${nonAsciiWsConsume('wc', false, '      ')}`);
+    emit(`      continue;`);
+    emit(`    }`);
+    emit(`${nonAsciiWsConsume('cc', true, '    ')}`);
+  }
   if (templateToken) {
@@ -466,2 +565,13 @@ export function emitSoaLexer(grammar: CstGrammar, st: LexerSymtab): string | nul
   const emitMatcherTry = (m: (typeof matchers)[number], ind: string) => {
+    if (m.span) {
+      emit(`${ind}{ const _e = ${m.spanFn}(source, pos);`);
+      emit(`${ind}  if (_e >= 0) {`);
+      emit(`${ind}    tkPush(${m.k}, 0, pos, _e);`);
+      emit(`${ind}    pos = _e;`);
+      emit(`${ind}    continue;`);
+      emit(`${ind}  }`);
+      emit(`${ind}  if (_e === -2 && lexWindowMore) throw LEX_RETRY;`);
+      emit(`${ind}}`);
+      return;
+    }
     emit(`${ind}${m.re}.lastIndex = pos;`);
diff --git a/src/emit-parser.ts b/src/emit-parser.ts
index 74feb92..e120549 100644
--- a/src/emit-parser.ts
+++ b/src/emit-parser.ts
@@ -1149,5 +1149,4 @@ export function emitJsParser(grammar: CstGrammar, lexSrc: string | null): string
   e.emit(`// GENERATED by src/emit-parser.ts — do not edit. Specialized parser for grammar ${J(grammar.name ?? '')}.`);
-  // The lexer: EMITTED (specialized, standalone — see emit-lexer.ts) when the grammar
-  // is a plain token stream; the data-driven createLexer runtime otherwise
-  // (markup/indent/newline state machines stay interpreter-only).
+  // The lexer: EMITTED (specialized, standalone — see emit-lexer.ts) for token streams
+  // and the simple newline-only mode; the data-driven createLexer runtime otherwise.
   // `lexSrc` is handed in by the Target façade (emitParser reuses emitLexer) — see emit.ts.
@@ -2623,3 +2622,3 @@ function visitCore(entry: number, fns: _VisitFns, charBase?: number, tokBase?: n
 function lexInto(source: string) {
-${e.soa ? `  tokenize(source);
+${e.soa ? `  lexSource(source);
   docEmptyPops = lexEmptyPops.slice();` : String.raw`  docPieces = [source]; docPieceOff = [0]; docLen = source.length; docFlat = source; docCur = 0;
@@ -4054,2 +4053,8 @@ ${e.soa ? String.raw`  // ── M1: WINDOWED re-lex ──
-export { tokenize };
+${e.soa ? `export function tokenize(source: string) {
+  activate(docDefault);
+  lexSource(source);
+  const tokens = new Array(tokN);
+  for (let i = 0; i < tokN; i++) tokens[i] = tokenAt(i);
+  return tokens;
+}` : 'export { tokenize };'}
 // ── Module-level API: the DEFAULT document (one shared session; tokenize and the
