{
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "pnpm --filter satorigear... build",
    "dev": "pnpm --filter satorigear... --parallel dev",
    "prepare": "zin link satorigear",
    "prepare:vendor": "node scripts/vendor.ts",
    "release": "bumpp -r --no-push -c \"release: v%s\"",
    "test": "vitest",
    "typecheck": "tsc",
    "benchmark": "pnpm build && node --expose-gc benchmark/parse.bench.ts",
    "benchmark:incremental": "pnpm build && node --expose-gc benchmark/edit.bench.ts",
    "test:markdown": "vitest run test/mdast.test.ts test/document.test.ts",
    "conformance:markdown": "vitest run test/mdast-conformance.test.ts test/incremental-fuzz.test.ts --reporter=verbose"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@zinkawaii/eslint-config": "catalog:",
    "@zinkawaii/scripts": "catalog:default",
    "@zinkawaii/tsconfig": "catalog:",
    "bumpp": "catalog:",
    "commonmark-spec": "catalog:",
    "entities": "catalog:",
    "eslint": "catalog:",
    "mdast-util-from-markdown": "catalog:",
    "mitata": "catalog:",
    "remark": "catalog:",
    "satteri": "catalog:",
    "tsdown": "catalog:",
    "unrun": "catalog:",
    "vitest": "catalog:"
  }
}
