{
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "pnpm codegen && pnpm --filter satorigear... build",
    "dev": "pnpm codegen && pnpm --filter satorigear... --parallel dev",
    "codegen": "unrun scripts/parser.mjs",
    "prepare": "zin link satorigear",
    "prepare:vendor": "node scripts/vendor.ts",
    "release": "bumpp -r --no-push -c \"release: v%s\"",
    "test": "pnpm codegen && vitest",
    "typecheck": "pnpm codegen && tsc",
    "benchmark": "pnpm build && node --expose-gc benchmark/parse.bench.ts",
    "benchmark:incremental": "pnpm build && node --expose-gc benchmark/edit.bench.ts",
    "test:markdown": "pnpm codegen && vitest run test/markdown-mdast.test.ts test/markdown-document.test.ts",
    "conformance:markdown": "pnpm codegen && vitest run test/markdown-mdast-conformance.test.ts test/markdown-incremental-fuzz.test.ts --reporter=verbose"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@zinkawaii/eslint-config": "catalog:",
    "@zinkawaii/scripts": "catalog:default",
    "@zinkawaii/tsconfig": "catalog:",
    "bumpp": "catalog:",
    "commonmark-spec": "catalog:",
    "entities": "catalog:",
    "eslint": "catalog:",
    "mdast-util-from-markdown": "catalog:",
    "mitata": "catalog:",
    "remark": "catalog:",
    "satteri": "catalog:",
    "tsdown": "catalog:",
    "unrun": "catalog:",
    "vitest": "catalog:"
  }
}
